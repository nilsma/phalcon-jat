<div id="main-container">

    <?php $this->partial('partials/memberHeader'); ?>

    <div id="inner-container">

        <?php echo $this->flash->output() . '<br/>'; ?>

        <?php echo $this->partial('partials/controllers-template') . "\n"; ?>

        <div id="contacts" class="container">

            <?php

            if(count($contacts) > 0) {

                if($user->view_type == "LIST") {
                    echo '<div class="container">' . "\n";
                    echo $this->partial('partials/overview-contact-list-headers') . "\n";
                }

                foreach($contacts as $contact) {

                    $form = new OverviewContactForm($contact);

                    if($user->view_type == "OVERVIEW") {
                        echo $this->partial('partials/overview-contact-details-template') . "\n";
                    } else {
                        echo $this->partial('partials/overview-contact-list-template') . "\n";
                    }

                }

                if($user->view_type == "LIST") {
                    echo '</div>' . "\n";
                }

            } else {

                echo $this->partial('partials/missing-entries') . "\n";

            }

            ?>

        </div> <!-- end #contacts -->

    </div> <!-- end #inner-container -->

</div> <!-- end #main-container -->